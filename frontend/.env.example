# Copy this file to .env.local and adjust if needed.
# Variables prefixed with NEXT_PUBLIC_ are sent to the browser — never put secrets here.

# Base URL of our Express backend
NEXT_PUBLIC_API_URL=http://localhost:4000
